# その手があったか — 制作憲法

平日毎日1本、6分の音声番組と、独立した note 記事を作る。
詳細仕様は `docs/plan.md`（v3.5）。**矛盾したら plan.md が勝つ。**

## 1. 番組の核

- 表の問い：**なんで、それで儲かるんですか？**（構造）
- 裏の問い：**なんで、その人がやったんですか？**（動機）
- 聞き手：大企業・中堅企業の新規事業担当。通勤中、耳だけ。「困っている人」ではなく「まだ火が残っている人」として扱う

### 固定文言（一字一句変えない）

- OP：`はい、始まりました。『その手があったか』。世の中の商売の、その手を持ち帰る時間です。◯◯です。`
- ED：`きょうの手は、△△、でした。あなたの業界の当たり前、ひとつだけ動かすなら、どこですか。答えがひとつでも浮かんだら、それが、あなたの企画書の一行目です。それでは、また明日。◯◯でした。`

## 2. 禁止事項（全出力に適用）

1. 「大企業だから仕方ない」等、諦めの正当化／会社・上司の悪口による共感取り
2. 説教（「挑戦しろ」の直接表現）・講義口調・見下し
3. 「情熱があれば」等の精神論での締め
4. カタカナ手法名（デザイン思考・破壊的イノベーション等）※note のみ可
5. 事実カードに対応しない数字・固有名詞
6. 個人の失敗を嗤う／個人の人格を扱う（会社と構造は扱う）。**うまくいかなかった時期を「失敗」と値踏みしない。**本人・当事者・信頼できる媒体がそう書いていない限り、評価語ではなく出典にある事実（数字・出来事）で書く
7. ダジャレ・キャラ芸
8. 「いかがでしたか」等の定型句／台本内の注釈・免責・出典読み上げ
9. **決め台詞・警句・ポエム調のまとめ**（対句、体言止めの要約、「◯◯は、◇◇に隠れる」型）
10. **「香月」の語を台本・TTS稿・note に出さない**（制作上の人格。放送には登場させない）
11. **note にラジオ・放送・番組・音声・リスナーへの言及や導線を置かない**（独立媒体）
12. 読者募集・拡散施策（読者数・PV は目的ではない）
13. **外へ出るものに制作手順と選定基準を出さない。** 手・動機・風の記号（OP／MO／TW）とその一覧名、「トリガ」「掛け味」「事実カード」「数字の系統」等の内部用語を書かない。**分類体系はこちらの持ち物で、読者・聞き手に見せるものではない**

| | 対象 | 扱い |
|---|---|---|
| **外に出る** | note記事・配信概要欄・**台本本文**（将来そのまま音声になる） | 制作情報を混ぜない。`scripts/check.py` が機械判定で弾く |
| **内に残す** | brief・事実カード・構成表・検査レポート・台帳・ボツネタ棚・制作アーカイブ | **詳しいまま残す。**人間が判断と追跡をするための道具なので、削らない |

台本の見出しブロック（`---` より上）は読み上げ対象外なので、制作情報を書いてよい。

## 3. 芸の土台（両ホスト共通）

**わからない側の代表。** 素朴な質問を先に投げ、返ってきた答えを自分の言葉と生活の実例で言い直し、「ってことですよね」と確かめて進む。知らないことは知らないと言う。卑下は口にしない。
うまい警句で締めるのは芸ではない。**わかっていく過程を聞かせること**が芸。

出典に触れるときは**主語なしの取材話法**のみ：`調べてみたら` `聞いてみたところ` `あとで確かめたら` ＋ 伝聞の `〜だそうです` `〜とのことでした`。発表主体・役職名は言ってよい。個人名・媒体名・スタッフ名は出さない。

**この取材話法は放送だけのルール。note には持ち込まない。** 記事は独立媒体で出典も列挙するので、企業名・調査名・賞の名前・公表された人物名は書く。伏せると「書けない事情があるのか」と読まれ、実名より信頼が下がる。

詳細 → `personas/tokura.md` `personas/misaki.md` `personas/kazuki.md`

## 4. 鮮度（絶対条件）

**トリガ（本題に関連する1ヶ月以内の公開物：ニュース・リリース・論文・統計・決算・法制化）が必ず存在すること。** 本題自体の日付は古くてよい。
1ヶ月を超える情報は、台本・記事の中で時期を明示する（「去年の八月」「一昨年の調査」）。聞き手は「この1ヶ月のニュース」のつもりで聞いている。
複数トピック回は、トピックごとにトリガを紐づける。トリガのないネタは捨てず `ledger/backlog.yaml` に寝かせる。

### ボツネタは捨てない

巡回で挙がって選ばなかった候補は、**全部 `ledger/backlog.yaml` に残す。** どんな話だったか（`gist`）、見送った理由（`status`）、何が起きたら復活するか（`revive_when`）を必ず書く。理由が「鮮度切れ」なのか「手が立たない」のかで、次に拾える時期が変わるため。
**一覧に載せる前に落とした種も書く**（`status: idea`）。手と畑と主役と技術度（`hand` `field` `subject` `tech`）も入れておくと、冷えている軸が分かったときに棚から直接引ける。
`gist` は普通の日本語で1〜2文。**半年後に棚を眺めたとき、そこだけ読めば中身を思い出せること**が条件で、タイトルと題材名だけでは思い出せない。
`/patrol` は毎回、**新しく探す前にこの棚を読む。** 同じネタを二度探し直さないための仕組み。

## 5. ワークフロー

```
/patrol            → spread.py でばらつきを見る → 6つの巡回棚をスキャン
                   → ledger照合 → 候補を1件ずつ採点して提示
（人間が1本選ぶ。選んだ回を ledger/selection.yaml に記録）
/episode <id>      → 以下を順に実行
  1. extract-facts   sources/ → facts.yaml（★以降 原文は渡さない）
  2. write-outline   brief+facts → outline.md
  3. write-script    outline+persona+過去回 → script_draft.md
  4. review-episode  scripts/check.py 実行 → review.md（FAILなら3へ差し戻し・最大2周）
  5. tts-format      draft+dictionary → script_tts.txt
  6. write-article   brief+facts → article.md + shownotes.md → 再レビュー
  7. write-infographic facts+article → infographic.html（省略不可）
  8. write-figures   article → figures.html（省略不可。note に貼る図版3枚）
  9. build_site.py   docs/ を再生成（出力が「自動」なら7が漏れている）
 10. export_figures.py → docs/<id>/images/ に PNG。そのあと build_site.py を再実行
（人間が選ばなかった候補は ledger/backlog.yaml へ書き戻す）
```

### git の進め方：1変更＝1ブランチ＝1PR

**マージ済みのブランチを使い回さない。** 変更のたびに `main` から新しいブランチを切る。

```
git fetch origin main
git checkout -B claude/<内容> origin/main     # 例: claude/ep010, claude/fix-tts-dict
（作業・コミット）
git push -u origin claude/<内容>              # → PR を作る
```

- ブランチ名は内容が分かる形（`claude/ep010`・`claude/backlog-page`）
- **push する前に必ず `git fetch` して、前のPRがマージ済みでないか確認する。** マージ後に同じブランチへ押すと、PRの無いコミットが取り残される
- 1PRに複数テーマを混ぜない。読む側が判断できる単位で分ける
- **公開の可否は人間が決める。** マージは人間が行い、エージェントは勝手にマージしない

## 6. 数値ゲートは必ずスクリプトで

文字数・並走句・禁止語・定型文一致は**目視で判断しない**。必ず実行する：

```bash
python3 scripts/check.py episodes/<id>
```

読み辞書の回収・適用漏れも機械の仕事：

```bash
python3 scripts/dictionary.py                    # 適用漏れと未登録の英字を見る
python3 scripts/dictionary.py --collect --write  # その回で開いた語を辞書へ回収
python3 scripts/dictionary.py --apply <id>       # 辞書を TTS 稿に当てる
```

数字系統数・固有名詞数・キメ台詞の有無は機械判定できないため、スクリプトの候補出力を見て**モデルが判断し、review.md に数え方の根拠を書く**。

## 7. 人間が必ずやること（省略不可）

1. brief.yaml の記入と、sources/ への素材投入
2. 単独出典・要約経由の数字の裏取り
3. **TTS生成後の通し聴取**（誤読した語を `library/dictionary.yaml` に追記する。
   **読みを決めるのは人間。**その回で開いた語の回収と、次の回への適用は `scripts/dictionary.py` が機械でやる）
4. 公開の承認

## 8. ファイル地図

| 用途 | 場所 |
|---|---|
| 全仕様 | `docs/plan.md` |
| ホスト・作家 | `personas/` |
| 手14・動機7・風5・型8 | `library/` |
| 読み辞書（育成型） | `library/dictionary.yaml` ／ 回収・検査・適用は `scripts/dictionary.py` |
| 巡回棚・リスク語 | `config/sources.yml` `config/ng.yml` |
| 放送済み台帳（重複回避） | `ledger/episodes_log.csv` |
| **ボツネタ棚（見送り理由と復活条件）** | `ledger/backlog.yaml` |
| **ネタの採点表（核2・面白さ5）** | `ledger/selection.yaml` → サイトの「選び方」 |
| **6つの点数**（見立て＝面白さ・応用・ばらつき／実測＝鮮度・裏取り・人物度） | `scripts/score.py`。足さない。低いものを見る |
| 選定のばらつき（手・動機・風・型・畑・主役・技術度） | `scripts/spread.py`（`episodes_log.csv` から計算） |
| note の公開URLと記事間の関連 | `ledger/note_links.yaml`（公開したらURLを書き足す） |
| note に貼る図版 | `episodes/<id>/figures.html` → `scripts/export_figures.py` → `docs/<id>/images/` |
| 閲覧サイトの生成 | `scripts/build_site.py` → `docs/` |
| **文体の教師データ** | `episodes/ep020`（間とフィラーの基準形）・`ep003`（聞き上手）・`ep005`（動機回）・`ep008`（拡大版15分） |
