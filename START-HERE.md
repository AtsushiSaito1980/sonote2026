# はじめかた

エンジニアでなくても大丈夫です。ファイルを自分で書く場面はありません。

---

## 0. 登場人物は3人だけ

| | 役割 | 例えると |
|---|---|---|
| **GitHub** | 原稿を置く場所 | 誰がいつ何を直したか全部残る、クラウドの本棚 |
| **Claude Code** | 作業する場所 | 本棚から原稿を借りて書き、「これでどうですか」と提案してくる編集室 |
| **あなた** | 決める人 | 提案を見て「OK」を押す。押した瞬間、本棚の正本が更新される |

Claude Code は **GitHub にある原稿を借りてくる**しくみです。
だから最初に一度だけ、原稿を GitHub に置く作業が要ります。**そこだけパソコンを使います。**
それが終われば、あとはずっとスマホで回せます。

---

# 第1部　最初の1回だけの準備（30分・パソコン）

## Step 1　ファイルを取り出す

1. ダウンロードした `sonote-repo.tar.gz` をダブルクリック
2. `sonote` というフォルダができる。これが原稿一式です

## Step 2　GitHub に置く

GitHub のアカウントがまだなら、先に github.com で無料登録してください。

### 方法A：画面の操作だけでやる（おすすめ）

1. github.com にログインし、右上の **「＋」→ New repository**
2. 次のように入力
   - Repository name（名前）：`sonote`
   - **Private を選ぶ**（他人に見せない設定）
   - 「Add a README file」の**チェックは外す**
3. **Create repository** を押す
4. 次の画面に出てくる **「uploading an existing file」** の青い文字を押す
5. ⚠️ **ここが唯一の注意点です。** Finder で `sonote` フォルダを開き、キーボードで **⌘（コマンド）+ Shift + .（ピリオド）** を押してください。隠れていたファイルが薄く表示されます
6. `sonote` フォルダの**中身を全部選んで**（⌘+A）、GitHub の画面にドラッグ
7. アップロード一覧に **`.claude` と `.gitignore`** があるか確認してください
   - **無いとClaudeが指示書を読めません。**もう一度 Step 5 からやり直し
8. 下の **Commit changes** を押す

### 方法B：ターミナルが使えるなら（3行で終わる）

```bash
tar xzf sonote-repo.tar.gz && cd sonote
git init && git add -A && git commit -m "init"
gh repo create sonote --private --source=. --push
```

## Step 3　Claude Code とつなぐ

1. ブラウザで **claude.ai/code** を開く
2. GitHub との接続を求められるので、承認する
3. リポジトリの一覧から **`sonote`** を選ぶ

これで準備完了です。

## Step 4　ちゃんと動くか3つだけ試す

セッションを開いて、日本語でそのまま打ってください。

**テスト1：指示書が読めているか**
```
CLAUDE.mdを読んで、この番組の禁止事項を教えて
```
→ 12個の禁止事項が返ってくればOK。返らなければ `.claude` のアップロード漏れです

**テスト2：検算プログラムが動くか**
```
python3 scripts/check.py episodes/ep003 を実行して、結果を見せて
```
→ 「機械判定はすべて PASS」と出ればOK

**テスト3：ニュース巡回ができるか**
```
/patrol
```
→ 候補が5〜7本出ればOK。「ネットにつながらない」と言われたら、設定でネットワークアクセスの許可を広げます（Claudeに「ネットワーク設定はどうすればいい？」と聞けば案内してくれます）

---

# 第2部　毎日の流れ（15分・スマホ）

Claudeアプリ、またはスマホのブラウザで claude.ai/code を開き、`sonote` で新しいセッションを始めます。

## ① ネタを探す（1分）

```
/patrol
```

6つの分野のニュースを巡回して、今日の候補を5〜7本出してくれます。
過去に扱ったネタや、同じ手が続かないよう、自動で避けてくれます。

## ② 1本選ぶ（1分）← ここだけが人間の仕事

```
3番でいこう。ホストは三崎で。
```

**この選択があなたの一番大事な仕事です。** あとは全部Claudeがやります。
角度を指定したければ、こう足すだけ：

```
3番で。ただし制度の説明じゃなくて、それで儲けてる会社の側から見せて。
```

## ③ 作らせる（5分・待つだけ）

```
/episode ep007
```

Claudeがこの順に進めます。

1. 元記事を集める
2. 事実カードを作る（ここで元記事の文章を捨てる＝著作権対策）
3. 台本を書く
4. 検算プログラムで自己採点する
5. 落ちたら自分で書き直す
6. TTS用の原稿、note記事、概要欄を作る

## ④ 確認して「OK」を押す（5分）

```
PRを作って
```

**PR（プルリクエスト）＝「この原稿でどうですか」という提案書**のことです。
スマホの画面で中身を読み、良ければ **Merge** ボタンを押します。これで正本になります。

直したいときは、そのまま日本語で言えば直ります。

```
つかみの数字が弱いので、別の数字に変えて
```

## ⑤ 音声にする（3分・ここだけ手作業）

1. GitHubで `episodes/ep007/script_tts.txt` を開く
2. 全部コピー
3. ElevenLabsに貼って生成
4. **必ず一度、通して聴く**

読み間違いがあったら、セッションに戻ってこう言うだけ：

```
「施行」が「せこう」と読まれた。辞書に追加して
```

次回から自動で直ります。

## ⑥ 公開

音声と、`article.md` の中身をnoteへ。概要欄は `shownotes.md` をそのまま貼れます。

---

# 第3部　困ったときの早見表

| 症状 | 原因と対処 |
|---|---|
| Claudeが番組のルールを知らない | `.claude` フォルダが上がっていない。Step 2の5〜7をやり直す |
| 台本の口調がおかしい・キメ台詞が出る | 「episodes の直近3本を読んでから書き直して」と伝える |
| 文字数が足りないと言われる | 「未使用の事実カードを使って埋めて」と伝える。言い回しの水増しはさせない |
| 同じような回ばかり出る | 「ledger を見て、まだ使ってない手から選んで」と伝える |
| ニュースが集まらない | ネットワーク設定の許可範囲。Claudeに聞けば手順を案内してくれる |
| 何をすればいいか分からなくなった | セッションで「いま何をすればいい？」と聞く。CLAUDE.mdを読んで教えてくれる |

---

# 覚えておく言葉は4つだけ

| 言葉 | 意味 |
|---|---|
| **リポジトリ** | 原稿一式が入った、クラウドの箱 |
| **コミット** | 「この状態を記録して」＝原稿のセーブ |
| **PR** | 「この変更でどうですか」という提案書 |
| **マージ** | 提案書にOKを出すこと。これで正本になる |

---

# 最初の1週間の進め方

いきなり毎日やらないでください。

- **1日目**：Step 1〜4だけ。動作確認まで
- **2日目**：ep007を1本、最後まで通す。音声を聴くところまで
- **3〜4日目**：もう2本。口調が安定するか見る
- **5日目以降**：毎日1本の運用へ

**うまくいかない原因のほとんどは、Claudeではなく設定です。** 焦らず、動作確認から積み上げてください。
